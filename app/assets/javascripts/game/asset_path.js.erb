let useOfflineAssets = true;
<%= ENV["OFFLINE"] ? true : false %>;

function getAssetPath(scene) {
  let assetPath = 'https://s3-us-west-1.amazonaws.com/spaceblazer/';
  if (useOfflineAssets) {
    assetPath = '/offline/';
  }

  return assetPath;
}
